var Player = function(id, socket)
{
	this.id = id;
	this.x = 0.0;
	this.y = 0.0;
	this.socket = socket;
}

Player.prototype = {
	
	say: function(msg)
	{
		this.socket.emit('say', {id: this.id, msg: msg});
	}

	
}

module.exports = Player;